/**
 Transforms the 2d matrix image into FFT using the CPU.
 The image is represented as grayscale image, each value is from 0-100 and represents the brightness.
 */
void img2fftCPU(float** dest, int** img, int N){
}

/**
 Transforms the 2d matrix image into FFT using the GPU.
 */
void img2fftGPU(float** dest, int** img, int N){

}
